server:
  port: "8080"
  mode: "debug"

database:
  host: "localhost"
  port: 5432
  username: "postgres"
  password: "password"
  dbname: "easynode"
  sslmode: "disable"

docker:
  deployment_path: "../deployment"
  node_image: "."