# Quilibrium 技术深度解析：超图、PebbleDB、智能合约与 MPC

本文档旨在深入解答关于 Quilibrium 核心组件之间关系的一些关键问题。

## 1. 超图 (Hypergraph) 和 PebbleDB 的关系是什么？

超图和 PebbleDB 是两个不同层级的组件，它们之间是**应用层数据结构**与**底层物理存储引擎**的关系。

可以把它们类比为：
-   **PebbleDB**: 就像是计算机的**硬盘驱动器 (Hard Drive)**。它是一个高性能的键值存储（Key-Value Store）引擎，负责将数据以字节流的形式高效、持久地写入和读取到磁盘。它速度快、可靠，但它本身并**不理解**所存储数据的内在逻辑或关系。
-   **超图 (Hypergraph)**: 就像是运行在硬盘之上的**文件系统或数据库索引**。它是一个在内存中运行的高级数据结构，它定义了数据（顶点和超边）的逻辑关系和组织方式。它知道哪个顶点连接到哪条超边，如何处理添加和删除操作以保证最终一致性 (CRDT)。

**它们如何协同工作？**

1.  **运行时**: 当 Quilibrium 节点运行时，超图的逻辑结构存在于内存中，以便进行快速的查找、添加和修改操作。
2.  **持久化**: 当需要保存状态时（例如，在一个共识帧被确认后），`PebbleHypergraphStore` (定义于 `node/store/hypergraph.go`) 会介入。它的职责是将内存中复杂的超图结构**序列化**成 PebbleDB 可以理解的简单键值对。
    -   例如，它会为每个分片（Shard）创建特定的键，如 `hypergraph:vertex_adds:<shard_key>`，并将该分片中所有新添加的顶点集合（序列化为字节流）作为值存入 PebbleDB。删除集也同理。
3.  **加载时**: 当节点启动时，`PebbleHypergraphStore` 会反向操作：它从 PebbleDB 中读取所有相关的键值对，然后**反序列化**这些数据，在内存中重建出完整的、可操作的超图逻辑结构。

**总结**: **PebbleDB 提供动力和存储空间，而超图提供智能和结构。** 超图让数据变得有意义，而 PebbleDB 确保这些有意义的数据不会丢失。

---

## 2. 如何在 Quilibrium 上开发应用？数据如何存储？

在 Quilibrium 上开发链上应用或智能合约的方式与以太坊等平台有显著不同。

### 开发模式

Quilibrium 的链上应用逻辑被称为**内在函数 (Intrinsics)** 或内在合约。它们不是用一种独立的脚本语言（如 Solidity）编写的，而是直接用 **Go 语言**编写，并作为节点核心逻辑的一部分被编译进去。

-   **代码位置**: `node/execution/intrinsics/` 目录是核心地带。例如，`token/` 子目录中包含了完整的代币（Q-Token）实现，`token_execution_engine.go` 就是其“智能合约”的核心逻辑。
-   **开发流程 (推断)**:
    1.  开发者需要在 Go 中实现一套特定的接口，定义应用的执行逻辑。
    2.  这些逻辑会直接调用超图的 Go API (`hypergraph.AddVertex()`, `hypergraph.RemoveHyperedge()` 等) 来读取和修改状态。
    3.  应用代码需要与节点主程序一起被**重新编译**成一个新的节点二进制文件。
    4.  部署一个新的“智能合约”实际上是一次**网络范围的软件升级**，很可能需要通过项目的治理机制来协调。

### 应用数据的存储方���

是的，应用的数据**完全存储在超图上**。超图的结构天然适合表示复杂应用的状态。

-   **数据即顶点**: 应用的任何状态单元（例如一个用户账户、一个代币余额、一篇帖子）都可以被表示为一个或多个**顶点 (Vertex)**。
-   **关系即超边**: 应用的逻辑操作或数据间的关系则通过**超边 (Hyperedge)** 来表示。一条超边可以连接多个顶点，从而创建一个丰富的上下文。

**以代币转账为例**:
1.  Alice 和 Bob 的账户分别由不同的顶点表示。
2.  当 Alice 向 Bob 转账 10 个 Q 代币时，`TokenExecutionEngine` 会创建一个新的“转账”**超边**。
3.  这条超边会同时连接多个顶点：
    -   Alice 的账户顶点（作为来源）。
    -   Bob 的账户顶点（作为目标）。
    -   一个代表“10 Q”这个数量的顶点。
    -   可能还有代表交易时间戳、手续费等信息的其他顶点。

通过这种方式，所有的数据和它们之间的关系都被清晰地记录在分布式的超图中，而不是简单的键值对里。

---

## 3. 超图和 MPC (多方安全计算) 的联系

超图和 MPC 之间没有直接的实现依赖，但它们在 Quilibrium 的安全模型中扮演着相辅相成的关键角色，共同保障了数据的**机密性和可验证性**。

它们的联系可以这样理解：**MPC 用于创建和管理加密工具，而超图则是这些加密工具产生的数据（密文和证明）的应用和存储场所。**

**具体联系点：可验证加密 (Verifiable Encryption)**

在 `node/crypto/verifiable_encryption.go` 文件中，我们看到了一个名为 `MPCitHVerifiableEncryptor` 的结构体。这里的 "MPCitH" 很可能代表 **"Multi-Party Computation in the Hypergraph"**。

其协同工作流程如下：

1.  **保护隐私的需求**: 存储在超图上的某些应用数据需要保持机密，不能被全网所有节点公开读取（例如，一个加密聊天应用的内容，或一个需要保护隐私的交易细节）。
2.  **MPC 的作用**: 一组被选定的节点（委员会）可以使用 MPC 协议（如 `nekryptology` 库中实现的 FROST）进行**分布式密钥生成 (DKG)**。它们共同生成一个加密公钥，但私钥被分散成多个分片，由每个���与节点独立持有。完整的解密私钥从未在任何地方出现过。
3.  **加密与存储**:
    -   用户或应用可以使用这个共享公钥来加密数据。
    -   这个加密后的数据（密文）被作为一个**顶点**存储在公开的超图上。
4.  **验证与解密**:
    -   **验证**: 这种加密方案是“可验证的”，意味着它可以生成一个**零知识证明**，在不泄露原文的情况下，向全网证明这段密文满足某些特定属性（例如，“这个加密的数字小于 100”）。网络中的任何节点都可以验证这个证明的有效性。
    -   **解密**: 只有当**超过门限数量**的委员会节点合作时，它们才能使用各自的私钥分片共同解密超图上的这个密文顶点。

**总结**: MPC 为超图提供了一种强大的能力——**在公开、透明的分布式数据结构（超图）上存储不透明、但可验证的机密信息**。这使得 Quilibrium 能够构建兼具去中心化透明性和用户隐私保护的复杂应用。
