# 多方安全计算 (MPC) 在 Quilibrium 中的应用

## 1. 核心概念：什么是多方安全计算 (MPC)？

多方安全计算 (Multi-Party Computation, MPC) 是一套强大的密码学技术，它允许一组互不信任的参与方（在本项目中是网络节点）协同完成一次计算任务，而任何一方都无法获知其他参与方的私密输入。

简单来说，MPC 实现了“数据的可用而不可见”。大家可以共同得出一个计算结果（例如生成一个共享的签名密钥），但在这个过程中，每个参与者自己的秘密（例如自己的密钥分片）始终不会被泄露给其他人。

## 2. MPC 在 Quilibrium 中的核心用途

在 Quilibrium 这样高度依赖密码学的去中心化网络中，MPC 是实现**信任最小化**和**系统健壮性**的基石。其主要应用体现在以下两个方面：

### a. 分布式密钥生成 (Distributed Key Generation, DKG)

这是 MPC 在本项目中最关键的应用。DKG 协议允许多个节点共同创建一个**共享的公钥**和一个**分布式的私钥**。

-   **共享公钥**: 这个公钥是公开的，可以被网络中的任何人用来验证签名。
-   **分布式私钥**: 这是最核心的部分。完整的私钥**从始至终都不曾存在于任何一个单一的节点上**。取而代之的是，每个参与 DKG 的节点仅持有该私钥的一个**分片 (share)**。

这种机制彻底消除了单点故障。攻击者无法通过攻破单个节点来窃取整个系统的核心私钥，因为没有任何一个节点拥有它。

### b. 门限签名方案 (Threshold Signature Scheme, TSS)

基于 DKG 生成的分布式私钥，系统可以实现门限签名。这意味着，要想使用这个共享的私钥进行一次有效的签名操作（例如，批准一个全网共识的状态），必须有**超过特定门槛数量（T-out-of-N）**的节点共同参与。

例如，一个 `2/3` 的门限签名方案意味着，在一个由 90 个“主节点”组成的委员会中，必须至少有 60 个节点合作，才能生成一个有效的签名。任何少于 60 个节点的组合都无法完成���名。

## 3. MPC 的主要优势和功能

1.  **去中心化信任**: MPC 将信任从单个实体分散到一个节点委员会中。系统的安全性不再依赖于某个节点的诚实，而是依赖于大多数节点的集体诚实。
2.  **极高的安全性**: 消除了私钥被盗的风险。只要攻击者无法在同一时间攻破并控制超过门限数量的节点，系统的核心密钥就是安全的。
3.  **增强共识健壮性**: 通过要求一个节点委员会对关键状态（如信标、时钟帧）进行门限签名，Quilibrium 确保了这些状态的合法性和全网的认可度，使得恶意行为者极难伪造或篡改关键的共识数据。

## 4. 在项目中的代码实现

MPC 的相关逻辑主要分布在 `nekryptology/` 和 `node/crypto/` 目录中。

-   `nekryptology/`: 这是一个核心的密码学库，从其 `CHANGELOG.md` 中“为 MPC 协议建立新接口”的记录可以看出，它提供了 MPC 的底层协议实现。其中 `frost` 子目录表明项目可能采用了 **FROST (Flexible Round-Optimized Schnorr Threshold Signatures)** 协议，这是一种高效、灵活的 DKG 和门限签名方案���
-   `node/crypto/verifiable_encryption.go`: 此文件中的 `MPCitHVerifiableEncryptor` 等结构体表明，MPC 技术也被用于实现**可验证加密**，这是一种确保数据被正确加密且只有特定方能解密的高级密码学工具，进一步增强了系统的隐私和安全性。

## 5. 总结

在 Quilibrium 项目中，MPC 不仅仅是一个理论概念，而是其去中心化安全模型的核心支柱。通过分布式密钥生成和门限签名，MPC 确保了没有任何一个单一实体可以控制网络的核心功能，从而构建了一个真正健壮、抗审查、无需信任的系统。
